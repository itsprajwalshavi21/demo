// Write a code to check if two strings match, where one string contains wildcard characters
#include <bits/stdc++.h>
using namespace std;

bool isWildcard(string text, string pattern)
{
    int t = text.size();
    int p = pattern.size();
    for (int i = 0; i < p; i++)
    {
        for (int j = 0; j < t; j++)
        {
            
        }
        
    }
    
}

int main()
{
    
    return 0;
}